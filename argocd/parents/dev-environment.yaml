apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-environment-root
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/ot-central-team/gitops.git'
    targetRevision: HEAD
    path: argocd/dev                  # <── Points ONLY to argocd/dev!
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
